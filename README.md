# JSON Server

- Rename *db.sample.json* to ***db.json*** 
- Run from terminal:
```
python3 server.py [options]

Options:
    --host          hostname or ip address      [default: localhost]
    --port, -p      port number                 [default: 8888]
    --data, -d      json data file              [default: db.json]
 
```
Requirements
-
* Python (3.8)

